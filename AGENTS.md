# AGENTS.md

## 適用範囲

- 本ファイルはリポジトリ全体に適用する。
- 作業対象の下位ディレクトリに別の `AGENTS.md` がある場合は、本ファイルと併用し、対象ファイルに近い指示を優先する。
- このサイトはビルド工程を持たない静的サイトである。公開ページはルート直下の HTML、画像は `images/` 配下の用途別フォルダに置かれている。

## 公開対象とファイル構成

### 現行の公開ページ

- `index.html`: TOPページ。スライドショー、Gallery導線、更新履歴を含む。
- `profile.html`: Profileページ。
- `Policy.html`: 撮影ポリシーページ。ファイル名の大文字 `P` を維持する。
- `event.html`: ナビゲーション上の Scheduleページ。今後と終了済みのイベントを管理する。
- `gallery.html`: Galleryページ。Snap、Portrait、Videographyを管理する。
- `equipment.html`: Gearページ。
- `contact.html`: Contactページ。

### アセットと補助ファイル

- `images/add/`: 追加予定画像の一時受け入れフォルダ。画像追加作業では最初に確認する。
- `images/common/`: ナビゲーションアイコン、OGP画像、ファビコンなどのサイト共通画像。
- `images/snap/`: GalleryのSnap画像。
- `images/portrait/`: GalleryのPortrait画像。
- `images/gear/`: Gearページの機材画像。
- `images/profile/`: Profileページ固有の画像。
- `images/video/`: Videography用画像とサムネイル。
- `photos.json`: `NewGallery.html` と `NewGallery ver2.html` が参照するGallery派生データ。
- `extract_photos.py`: `gallery.html` から `photos.json` を生成する補助スクリプト。現状は絶対パスを含むため、使用時は実行環境と出力差分を必ず確認する。

### 試作・保管物

- `new_index.html`、`NewGallery.html`、`NewGallery ver2.html` は試作ページとして扱い、ユーザーから明示された場合を除き公開ページの変更対象にしない。
- `temp.js` と `_inverted_products/` は現行公開ページから参照されていないため、通常のHP編集では変更しない。
- `My hp.zip` は保管物として扱い、展開、更新、削除、コミットを行わない。ただし、ユーザーから明示的な指示がある場合を除く。
- 公開対象を判断する際は、ファイルの更新日時や名称だけでなく、現行ページのナビゲーションと参照関係を優先する。

## 変更前の手順

1. 対象ファイルと上位階層にあるすべての `AGENTS.md` を確認する。
2. `git status` と対象ファイルの差分を確認し、ユーザーの既存変更を上書きしない。
3. 対象ページだけでなく、共通ナビゲーション、日英表示、レスポンシブ表示への影響を確認する。
4. 変更は依頼を満たす最小範囲に限定する。無関係な整形、全面的な書き換え、ファイル名変更、アセット整理を同時に行わない。

## 大規模変更の承認規則

- 大きな改変が必要な場合は、コードやファイルを直ちに編集してはならない。
- 先に現状、変更対象、提案内容、実装手順、影響範囲、想定リスク、確認方法を提示し、ユーザーの明示的な承認を得てから実装する。
- 次のいずれかに該当する変更は大規模変更として扱う。
  - 複数の公開ページにまたがるレイアウトまたはデザインの刷新。
  - ナビゲーション、ページ構造、Galleryの表示方式、更新履歴の管理方式、Scheduleのカード構造を根本から変更する作業。
  - 新しいフレームワーク、ビルドツール、外部ライブラリ、CMS、データ管理方式の導入。
  - 公開ファイル名、URL、ディレクトリ構成の変更。
  - 大量の画像変換、リネーム、削除、または既存コンテンツの一括移行。
- 調査、原因確認、実装計画の作成までは承認前に実行してよい。大規模変更に該当しない局所的な文言修正や既存方式に沿った追加は、通常の編集として扱う。

## 共通実装規則

- 現行サイトは各HTML内にCSSとJavaScriptを持つ。小規模変更ではこの構成を維持し、ユーザーの承認なく共通CSSやフレームワークへ移行しない。
- ナビゲーションのページ名とリンクは、`Profile / Policy / Schedule / Gallery / Gear / Contact` および現行の相対パスを維持する。
- 日本語と英語は原則として対で更新し、既存の `.lang-ja` と `.lang-en`、言語切り替え、`mokusaku_lang` の状態保持を壊さない。
- ユーザーから提供されていない氏名、日付、撮影条件、イベント情報、URL、翻訳上の固有名詞を推測で追加しない。
- 外部リンクを新しいタブで開く場合は、既存方式に合わせて `target="_blank"` と `rel="noopener noreferrer"` を付ける。
- 画像には用途に合う `alt` を設定する。Gallery画像では既存の `data-highres`、Portraitの `data-caption`、`loading="lazy"` を維持する。
- 既存の相対URLとファイル名の大文字・小文字を厳密に維持する。GitHub Pages上では大文字・小文字の差が問題になる前提で扱う。
- ユーザーの指示がない限り、右クリック・ドラッグ防止、スクロール演出、ページ上部へ戻るボタン、言語切り替え、モバイル表示を削除しない。

## Galleryの必須規則

- 対象は `gallery.html` の `Snap` と `Portrait` の両セクションである。
- 初期表示は各セクション最大20枚とする。21枚目以降の古い写真は初期状態で非表示にし、各セクションの「もっと見る」から表示する。
- 写真は新しいものを先頭、古いものを後方に並べる。新しい写真を追加するときは、既存写真の末尾ではなく対象グリッドの先頭へ追加する。
- Galleryへ写真を追加する前に、対象画像の実ファイル形式と拡張子が `.webp` であることを必ず確認する。
- 対象画像がPNG、JPEG、JPGなどWebP以外の形式である場合は、WebPへ変換してから追加する。変換時は元画像と同じ幅・高さのピクセル数を維持し、リサイズ、切り抜き、アップスケールを行わない。エンコード品質を調整し、1ファイルあたり1MB未満（1,048,576バイト未満）となる範囲で可能な限り上限値に近い、最も高品質な出力を採用する。`gallery.html` からは変換後の `.webp` を参照する。
- HTML上には全写真を保持する。現在は `setupSeeMore(..., 20)` が21枚目以降へ `.is-hidden` を付与するため、この閾値20を変更しない。同等の方式へ変更する場合も、初期表示20枚という結果を必ず維持する。
- Snapは `#snap-see-more-container`、Portraitは `#portrait-see-more-container` と現在のボタン動作を維持する。
- 写真が20枚以下の場合は、そのセクションの「もっと見る」を表示しない。
- 「もっと見る」を押した場合は、そのセクションの21枚目以降だけを表示し、他セクションへ影響させない。
- Portrait写真を追加・変更する場合は、被写体名やキャラクター名を推測せず、提供された表記を `data-caption` に反映する。
- `photos.json` は現行の `gallery.html` 自体の表示元ではない。試作Galleryも更新する依頼がある場合だけ、`gallery.html` を正として同期し、件数と順序を照合する。

## TOPページ更新履歴の必須規則

- 対象は `index.html` の更新履歴である。
- 初期表示は新しい順に直近3件だけとする。4件目以降は「過去の更新を見る」に格納する。
- 新しい履歴は `.timeline-list` 内の先頭へ追加する。既存の履歴を削除して3件に減らしてはならない。
- 現在は `setupTimeline('timeline-list', 'timelineOlder', 'timelineToggle', 3)` が4件目以降を移動するため、閾値3を変更しない。同等の方式へ変更する場合も、初期表示3件という結果を維持する。
- 履歴が3件以下の場合は「過去の更新を見る」を表示しない。
- 各履歴の日付、見出し、説明は日本語と英語を対で用意する。日付や更新内容を推測で作成しない。
- Galleryへの写真追加、Scheduleの更新、Policyの更新、Gearへの機材追加を行った場合は、ユーザーから更新履歴について個別の指示がなくても、同じ作業内で `index.html` の更新履歴を必ず追加する。
- 上記の必須更新履歴の日付には、イベント日、撮影日、機材購入日ではなく、実際に編集を行った当日の日付をAsia/Tokyo基準の `YYYY.MM.DD` 形式で記載する。
- 同一作業で複数の必須対象を更新した場合は、更新内容を漏れなく記載した1件の履歴へまとめてよい。
- 上記以外の変更では、ユーザーから更新履歴追加の指示または明確な根拠がない限り、勝手に履歴を作成しない。

## Scheduleページの必須規則

- ナビゲーション上のScheduleページは `event.html` である。
- 「今後のイベント予定」と「過去のイベント」でカード構造を統一する。各カードは原則として日付、日英のイベント名、会場、公式サイト、ステータスを同じ順序で持つ。
- 今後のイベントは未来の日付順で並べ、次のリンク付きボタンを必ず設置する。

```html
<a href="contact.html" class="event-status status-open">
  <span class="lang-ja">撮影依頼受付中</span>
  <span class="lang-en">Photography Requests Open</span>
</a>
```

- 今後のイベントの「撮影依頼受付中」をクリックしたときは、同一タブで `contact.html` へ遷移させる。外部URL、SNSのDM、`#` にはリンクしない。
- 終了したイベントは過去のイベント欄へ移し、カードに `past` クラスを付ける。ステータスは次の非リンク要素にする。

```html
<div class="event-status status-closed">
  <span class="lang-ja">終了しました</span>
  <span class="lang-en">Closed</span>
</div>
```

- 終了ステータスに `<a>`、`href`、クリック処理を付けてはならない。会場地図や公式サイトのリンクは、正しいURLが提供されている場合に限り維持してよい。
- 今後のイベントが0件の場合だけ、現在の「予定されているイベントはありません」という案内を表示する。イベント追加時は案内を残したままにしない。
- イベントの終了判定は、依頼時にユーザーが示した状態を優先する。日付のみから不確実な状態を推測して公開内容を変更しない。

## アセット追加規則

- 画像を追加する作業では、他の場所を検索する前に必ず `images/add/` を確認し、ユーザーの指示に該当する画像があるか、ファイル名と画像内容の両方で照合する。
- `images/add/` に複数の候補があり対象を一意に特定できない場合は、推測で組み込まず、候補を提示してユーザーへ確認する。該当画像がない場合も、その事実を報告する。
- `images/add/` は一時受け入れフォルダであり、公開HTMLから恒久的に参照してはならない。
- 該当画像を指示どおり組み込み終えたら、用途に応じて `images/common/`、`images/snap/`、`images/portrait/`、`images/gear/`、`images/profile/`、`images/video/` の適切なフォルダへ移動し、HTML、CSS、JavaScript、JSONの参照パスを移動後のパスへ更新する。
- Gallery用の公開画像は `images/snap/` または `images/portrait/` に配置し、WebP形式を必須とする。WebP以外の画像は元の解像度（幅×高さのピクセル数）を維持し、解像度を下げずにWebPへ変換する。容量調整はエンコード品質で行い、1MB未満（1,048,576バイト未満）を厳守しつつ、品質を不必要に下げず可能な限り1MB直下に近づける。容量を増やす目的だけの不要なメタデータやパディングは追加しない。
- Videography用画像とサムネイルは `images/video/` に配置する。
- 同名の既存ファイルを上書きしない。置換が明示された場合を除き、新しい一意な名前を使用する。
- 未使用画像や旧形式の一括削除は行わない。参照の有無だけでは保管意図を断定できないためである。

## 完了前の確認

- 変更したHTMLのタグ、引用符、閉じタグ、CSS、JavaScriptに構文上の破損がないか確認する。
- 内部リンクと画像パスの参照先が存在するか確認する。
- 日本語と英語の両表示で、追加・変更した内容が欠けていないか確認する。
- デスクトップ幅とモバイル幅で、ナビゲーション、本文、ボタン、画像が操作可能か確認する。
- Gallery変更時は、SnapとPortraitを別々に数え、各20枚まで初期表示、21枚目以降は「もっと見る」に格納、20枚以下ではボタン非表示であることを確認する。
- Galleryへ追加した全画像について、実ファイル形式と拡張子がWebPであり、元画像と幅・高さのピクセル数が一致し、各ファイルサイズが1MB未満（1,048,576バイト未満）であることを確認する。さらに、容量制限内で可能な限り高いエンコード品質が選択されていることを確認する。
- 画像追加時は、使用した画像が `images/add/` に残っておらず、適切な用途別フォルダへ移動済みで、すべての参照が移動後のパスを指していることを確認する。
- TOP更新履歴変更時は、直近3件だけが初期表示され、4件目以降が「過去の更新を見る」で展開されることを確認する。
- Galleryへの写真追加、Scheduleの更新、Policyの更新、Gearへの機材追加を行った場合は、`index.html` の更新履歴に編集当日の日付で対応する履歴が追加されていることを確認する。
- Schedule変更時は、今後の全カードにContactへの「撮影依頼受付中」があり、終了済みの全カードの「終了しました」にリンクがないことを確認する。
- ブラウザ確認が可能な場合は、コンソールエラー、表示崩れ、クリック動作も確認する。
- 最後に `git diff` と `git status` を確認し、依頼外のファイルや保管物が変更されていないことを確認する。

## 作業報告

- 完了報告では、変更ファイル、実装内容、実施した確認、未確認事項または残るリスクを簡潔に記載する。
- 検証を実行できなかった場合は、実行済みと表現せず、その理由を明記する。
